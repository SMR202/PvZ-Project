#include "Levels.h"

Levels::Levels(int num) : plantFactory(), zombieFactory(), sunFactory(), time(), num(num), name(), description(), objectives(), challenges(), rewards(), wave(), shovel() {}

Levels::~Levels() {}
